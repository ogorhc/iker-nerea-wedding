import { ParallaxStack, type ParallaxLayer } from './ParallaxStack';

export const Parallax = () => {
  return (
    <ParallaxStack
      pinDuration={3500} // Total scroll distance in pixels
    />
  );
};
